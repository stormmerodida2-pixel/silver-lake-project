Django==6.0.6
djangorestframework
djangorestframework-simplejwt
django-cors-headers
Pillow
nh3
python-decouple
requests
xhtml2pdf

# Production deployment - all safe to leave installed but unused in local dev, since each only
# activates when its own env var is set (see settings/base.py and settings/.env.example).
gunicorn
whitenoise
dj-database-url
mysqlclient
django-storages[s3]
